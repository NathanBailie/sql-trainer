`Вывести количество занятых мест по каждому рейсу (trip), отсортировав результат по убыванию количества занятых мест.`

```sql
SELECT pit.trip AS trip,
	COUNT(*) AS COUNT
FROM Pass_in_trip pit
GROUP BY pit.trip
ORDER BY COUNT DESC
```
