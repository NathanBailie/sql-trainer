`Сколько различных кабинетов школы использовались 2 сентября 2019 года для проведения занятий?`

```sql
SELECT COUNT(DISTINCT s.classroom) AS COUNT
FROM Schedule s
WHERE YEAR(s.date) = '2019'
	AND DAY(s.date) = '2'
```
