`В какие города можно улететь из Парижа (Paris) и сколько времени это займёт?`

```sql
SELECT DISTINCT t.town_to AS town_to,
	TIME_FORMAT(TIMEDIFF(t.time_in, t.time_out), '%H:%i:%s') AS flight_time
FROM Trip t
WHERE LOWER(t.town_from) = 'paris';
```
