`Выведите название страны, где находится город «Salzburg»`

```sql
SELECT co.name AS country_name
FROM Countries co
	JOIN Regions r ON co.id = r.countryid
	JOIN Cities ci ON r.id = ci.regionid
WHERE LOWER(ci.name) = 'salzburg'
```
