`В какие города летал Bruce Willis`

```sql
SELECT DISTINCT t.town_to AS town_to
FROM Passenger p
	JOIN Pass_in_trip pit ON p.id = pit.passenger
	JOIN Trip t ON pit.trip = t.id
WHERE LOWER(p.name) = 'bruce willis'
```
